Place Flyway SQL migration scripts here for user-service.

Naming convention: V1__create_users_table.sql, V2__...sql, etc.

Example (create users table):
-- V1__create_users_table.sql
-- CREATE TABLE users (
--   id BIGINT PRIMARY KEY,
--   username VARCHAR(255) NOT NULL,
--   email VARCHAR(255) NOT NULL
-- );
